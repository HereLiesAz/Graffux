name: Release AAB to Play

# Temporarily disabled for the one commit that applies the ArrayDeque crash fix.
# The patch runner restores the exact pre-capture workflow before its fix commit lands.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporarily disabled while applying crash fix"
