<?xml version="1.0" encoding="utf-8"?>
<!-- Generated by branding/icons/build.py — edit the geometry in branding/icons/sets/, not this file. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="?attr/colorControlNormal">
    <!-- layer-duplicate: One plate, and the same plate again below it and out of register — two, not three, because three plates in this family is the stack itself. Drawn as two big offset squares with one solid, it was the same picture as the boolean union in the selection family, and it stood outside the layer family's language. Drawn as three plates it was the plain layers glyph with a different plate filled in, and a rater called the two of them one icon in two states. The copy overlaps the original rather than sitting clear below it: clear of it, the pair was the merge-down glyph, which is also one plate over one solid slab. -->
    <!-- lineage (PS/AI/PR): Photoshop's Duplicate Layer — the stack, with the copy landing on top of it. -->
    <path
        android:fillColor="@android:color/transparent"
        android:strokeColor="@android:color/white"
        android:strokeWidth="1.4"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:pathData="M5.8,3.4L20.6,3.4L18.2,7.8L3.4,7.8Z" />
    <path
        android:fillColor="@android:color/white"
        android:pathData="M10,6.6L21.4,6.6L19,11L7.6,11Z" />
</vector>
