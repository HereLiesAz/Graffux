<?xml version="1.0" encoding="utf-8"?>
<!-- Generated by branding/icons/build.py — edit the geometry in branding/icons/sets/, not this file. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="?attr/colorControlNormal">
    <!-- mask: One plate, half of it withheld. A rater calls this a half-charged battery, or an open book, because a rectangle part-filled along one edge is a fill gauge before it is anything else — the same thing that kept the eraser from being an eraser. It is kept anyway, because the alternative was worse: given a hand-painted zigzag boundary instead of a ruled one, the notch and the wedge inside it made a bat silhouette, and a rater named the DC mark. A diagonal split was not available either — that is view-actual-colors. Neutral and wrong beats close to a trademark. -->
    <!-- lineage (PS/PR): Photoshop's layer-mask thumbnail — half revealed, half concealed. -->
    <path
        android:fillColor="@android:color/transparent"
        android:strokeColor="@android:color/white"
        android:strokeWidth="1.4"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:pathData="M3,4.6L21,4.6L21,19.4L3,19.4Z" />
    <path
        android:fillColor="@android:color/transparent"
        android:strokeColor="@android:color/white"
        android:strokeWidth="1.4"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:pathData="M12,4.6L12,19.4" />
    <path
        android:fillColor="@android:color/white"
        android:pathData="M12,4.6L21,4.6L21,19.4L12,19.4Z" />
</vector>
